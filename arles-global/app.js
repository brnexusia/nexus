const reveal = new IntersectionObserver((entries)=>{ entries.forEach(entry=>{ if(entry.isIntersecting){ entry.target.classList.add('visible'); reveal.unobserve(entry.target); } }); },{threshold:.08}); document.querySelectorAll('.reveal').forEach(el=>{ if(!el.classList.contains('visible')) reveal.observe(el); }); document.querySelectorAll('.nav-links a').forEach(a=>{ a.addEventListener('click',()=>document.getElementById('nav').classList.remove('open')); }); const board=document.querySelector('.activity-board'); if(board && window.matchMedia('(pointer:fine)').matches){ board.addEventListener('mousemove',(e)=>{const r=board.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;board.style.transform=`translate(${x*3}px,${y*3}px)`;}); board.addEventListener('mouseleave',()=>board.style.transform=''); }